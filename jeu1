<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion Sécurisée</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; padding: 50px; }
        .container { width: 300px; margin: auto; padding: 20px; border: 1px solid #ccc; border-radius: 10px; }
        input { width: 100%; padding: 10px; margin: 10px 0; }
        button { background-color: red; color: white; padding: 10px; border: none; cursor: pointer; }
    </style>
</head>
<body>
    <h2>Connexion Sécurisée</h2>
    <div class="container">
        <form id="phishingForm">
            <input type="email" id="email" placeholder="Votre email" required>
            <input type="text" id="prenom" placeholder="Votre prénom" required>
            <button type="submit">Se connecter</button>
        </form>
    </div>
    <script>
        document.getElementById("phishingForm").addEventListener("submit", function(event) {
            event.preventDefault();
            let email = document.getElementById("email").value;
            let prenom = document.getElementById("prenom").value;
            console.log("Email saisi :", email);
            console.log("Prénom saisi :", prenom);
            alert("Attention ! Vous venez de saisir vos informations sur un faux site. Ne faites jamais cela sur un site suspect !");
        });
    </script>
</body>
</html>
